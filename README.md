Rearrange
=========

This module is used for rearranging names.
Turns "LastName, FirtsName" into "FirtsName LastName"

# Example

Calling `rearrange_name("Turing, Alan") ` will return `"Alan Turing"`
